{
  "name": "the-ghost-of-galm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "audio:manifest": "node scripts/build-pixy-manifest.mjs",
    "audio:check": "node scripts/build-pixy-manifest.mjs --check",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
